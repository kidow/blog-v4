```tsx title="components/Timeline/index.tsx"
import classNames from 'classnames'
import type { FC, ReactNode } from 'react'

interface Props {
  list: Array<{
    tag?: string
    title?: string
    content: ReactNode
  }>
}

const Timeline: FC<Props> = ({ list }) => {
  return (
    <div className="relative">
      {list.map((item, key) => (
        <div
          key={key}
          className={classNames(
            "pl-5 relative -top-2 flex gap-5 after:content-[''] after:absolute after:w-3 after:h-3 after:-left-[5px] after:bg-white after:border-2 after:top-2 after:rounded-full after:border-blue-600 after:z-10 pb-10 last:pb-0",
            {
              "before:content-[''] before:absolute before:w-px before:bg-gray-300 before:top-2 before:-bottom-2 before:left-0":
                key !== list.length - 1
            }
          )}
        >
          {!!item.tag && (
            <div className="whitespace-nowrap font-semibold">{item.tag}</div>
          )}
          <div>
            <h2 className="font-semibold">{item.title}</h2>
            {item.content}
          </div>
        </div>
      ))}
    </div>
  )
}

export default Timeline
```

## Example

<iframe
  src="https://codesandbox.io/embed/blog-v4-ub7ie?fontsize=14&hidenavigation=1&initialpath=%2Ftimeline&theme=dark&view=preview"
  style={{
    width: '100%',
    height: 500,
    border: 0,
    borderRadius: 4,
    overflow: 'hidden'
  }}
  title="Blog-v4"
  allow="accelerometer; ambient-light-sensor; camera; encrypted-media; geolocation; gyroscope; hid; microphone; midi; payment; usb; vr; xr-spatial-tracking"
  sandbox="allow-forms allow-modals allow-popups allow-presentation allow-same-origin allow-scripts"
></iframe>
