import { Required, Codesandbox } from '@site/src/components'

## Code

```tsx title="components/Input/index.tsx"
import type {
  FC,
  ReactNode,
  DetailedHTMLProps,
  InputHTMLAttributes,
  KeyboardEvent
} from 'react'
import { useMemo } from 'react'
import classnames from 'classnames'
import { getRandomString } from 'services'

export interface Props
  extends Omit<
    DetailedHTMLProps<InputHTMLAttributes<HTMLInputElement>, HTMLInputElement>,
    'size' | 'prefix'
  > {
  size?: 'xs' | 'sm' | 'md' | 'lg'
  onEnter?: () => void
  prefix?: ReactNode
  suffix?: ReactNode
  align?: 'left' | 'right'
  error?: ReactNode
  fullWidth?: boolean
  info?: ReactNode
}

const Input: FC<Props> = ({
  size = 'md',
  className,
  onEnter,
  prefix,
  suffix,
  align = 'left',
  error,
  fullWidth = false,
  info,
  ...props
}) => {
  const onKeyDown = (e: KeyboardEvent<HTMLInputElement>) => {
    if (e.key === 'Enter' && !!onEnter) onEnter()
  }
  const randomId = useMemo(() => getRandomString(), [])
  if (prefix || suffix) {
    return (
      <div
        className={classnames(
          'inline-flex justify-between items-center border border-gray-300 rounded',
          {
            'bg-gray-100': props.disabled || props.readOnly,
            'bg-white': !props.disabled && !props.readOnly,
            'cursor-not-allowed': props.disabled,
            'cursor-default': props.readOnly,
            'text-xs p-1 rounded-sm': size === 'xs',
            'text-sm py-1 px-2 rounded': size === 'sm',
            'text-base py-2 px-3 rounded-md': size === 'md',
            'text-lg py-3 px-4 rounded-md': size === 'lg'
          },
          className
        )}
      >
        {prefix}
        <input
          {...props}
          className={classnames(
            'border-0 focus:outline-none mx-1 flex-1 w-full',
            {
              'text-right': align === 'right',
              'bg-gray-100': props.disabled || props.readOnly
            }
          )}
          onKeyDown={onKeyDown}
          spellCheck={false}
        />
        {suffix}
      </div>
    )
  }
  return (
    <>
      <div
        className={classnames('inline-block', {
          'w-full': fullWidth,
          relative: !!props.placeholder
        })}
      >
        {prefix}
        <input
          {...props}
          id={props.id || randomId}
          className={classnames(
            'block',
            {
              'bg-gray-100 cursor-not-allowed': props.disabled,
              'bg-white': !props.disabled && !props.readOnly,
              'cursor-default': props.readOnly,
              'text-xs p-1 rounded-sm': size === 'xs',
              'text-sm py-1 px-2 rounded': size === 'sm',
              'text-base py-2 px-3 rounded-md': size === 'md',
              'text-lg py-3 px-4 rounded-md': size === 'lg',
              'text-right': align === 'right',
              'w-full': fullWidth,
              'peer placeholder-transparent': !!props.placeholder
            },
            !!error ? 'border-red-500' : 'focus:border-primary border-gray-300',
            className
          )}
          onKeyDown={onKeyDown}
          spellCheck={false}
        />
        {!!props.placeholder && (
          <label
            htmlFor={props.id || randomId}
            className={classnames(
              'absolute truncate select-none max-w-[calc(100%-24px)] cursor-text -top-6 text-gray-600 text-sm transition-all peer-placeholder-shown:text-base peer-placeholder-shown:text-gray-400 peer-placeholder-shown:top-2 peer-focus:max-w-full peer-focus:left-0 peer-focus:-top-6 peer-focus:text-gray-600 peer-focus:text-sm',
              !!props.value ? 'left-0' : 'left-3'
            )}
          >
            {props.placeholder}
          </label>
        )}
        {suffix}
      </div>
      {(!!error || !!info) && (
        <div
          className={classnames('mt-1 text-xs', {
            'text-red-500': !!error,
            'text-gray-400': !!info
          })}
        >
          {error || info}
        </div>
      )}
    </>
  )
}

export default Input
```

## Props

## Example

<Codesandbox name="input" />
