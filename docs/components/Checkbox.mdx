import { Required, Codesandbox } from '@site/src/components'

## Code

```tsx title="components/Checkbox/index.tsx"
import type { FC } from 'react'

export interface Props {
  checked: boolean
  onChange: (checked: boolean) => void
  label: string
}

const Checkbox: FC<Props> = ({ checked, onChange, label }) => {
  return (
    <label className="inline-flex items-center mb-0 cursor-pointer">
      <input
        onChange={(e) => onChange(e.target.checked)}
        type="checkbox"
        checked={checked}
      />
      <span className="ml-1.5 text-sm">{label}</span>
    </label>
  )
}

export default Checkbox
```

## Props

| Name                | Type     | Default |
| ------------------- | -------- | ------- |
| checked<Required/>  | boolean  |         |
| onChange<Required/> | function |         |
| label<Required/>    | string   |         |

## Example

<Codesandbox name="checkbox" />
